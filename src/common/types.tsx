export interface IChildren {
    children: React.ReactNode
}
export interface IDate {
    day?: number,
    month: number,
    year: number
}
export interface IOption {
    value: number,
    label: string
}